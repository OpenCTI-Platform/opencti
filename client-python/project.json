{
  "name": "client-python",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "client-python",
  "projectType": "library",
  "targets": {
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": ".",
        "command": "uv sync --locked --package pycti"
      },
      "metadata": {
        "description": "Install the OpenCTI Python client dependencies"
      }
    }
  }
}