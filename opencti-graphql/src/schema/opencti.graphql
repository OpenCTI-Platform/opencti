### Scalar
scalar DateTime

### User
type User {
    id: ID!
    username: String
    email: String!
    created_at: DateTime
    updated_at: DateTime
    roles: [String]
}

input AddUserInput {
    username: String!
    email: String!
}

### QUERIES & MUTATIONS
type Query {
    # User
    users(first: Int, offset: Int): [User]
    user(id: String!): User
    me: User
}

type Mutation {
    # User
    addUser(input: AddUserInput): User
}