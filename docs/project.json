{
  "name": "docs",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "docs",
  "projectType": "application",
  "targets": {
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": ".",
        "command": "uv sync --locked --only-group docs"
      },
      "metadata": {
        "description": "Install the OpenCTI Documentation dependencies"
      }
    }
  }
}
