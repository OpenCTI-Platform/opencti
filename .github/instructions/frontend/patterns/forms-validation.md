# Forms & Validation

- **Stack**: `Formik` + `Yup`.
- **Hooks**: `useApiMutation` for submitting.
- **Validation**: Use helpers like `useDynamicSchemaCreationValidation` for entity schemas.
- **UI**: Wrap forms in `Drawer` components for creation flows.
