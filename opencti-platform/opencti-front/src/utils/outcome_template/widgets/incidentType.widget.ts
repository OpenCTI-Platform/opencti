import type { TemplateWidget } from '../template';

const widgetIncidentType: TemplateWidget = {
  name: 'Incident Type',
  widget: {
    type: 'attribute',
    id: 'widgetIncidentType',
    perspective: null,
    dataSelection: [],
  },
};

export default widgetIncidentType;
