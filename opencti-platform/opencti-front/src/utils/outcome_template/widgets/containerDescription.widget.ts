import type { TemplateWidget } from '../template';

const widgetContainerDescription: TemplateWidget = {
  name: 'Container Description',
  widget: {
    type: 'attribute',
    id: 'widgetContainerDescription',
    perspective: null,
    dataSelection: [],
  },
};

export default widgetContainerDescription;
