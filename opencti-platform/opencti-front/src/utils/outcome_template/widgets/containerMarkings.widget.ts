import type { TemplateWidget } from '../template';

const widgetContainerMarkings: TemplateWidget = {
  name: 'Container Markings',
  widget: {
    type: 'attribute',
    id: 'widgetContainerMarkings',
    perspective: null,
    dataSelection: [],
  },
};

export default widgetContainerMarkings;
