import type { TemplateWidget } from '../template';

const widgetIncidentSeverity: TemplateWidget = {
  name: 'Incident Severity',
  widget: {
    type: 'attribute',
    id: 'widgetIncidentSeverity',
    perspective: null,
    dataSelection: [],
  },
};

export default widgetIncidentSeverity;
