import type { TemplateWidget } from '../template';

const widgetContainerLabels: TemplateWidget = {
  name: 'Container Labels',
  widget: {
    type: 'attribute',
    id: 'widgetContainerLabels',
    perspective: null,
    dataSelection: [],
  },
};

export default widgetContainerLabels;
