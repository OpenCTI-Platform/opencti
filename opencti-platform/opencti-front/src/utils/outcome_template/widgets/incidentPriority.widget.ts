import type { TemplateWidget } from '../template';

const widgetIncidentPriority: TemplateWidget = {
  name: 'Incident Priority',
  widget: {
    type: 'attribute',
    id: 'widgetIncidentPriority',
    perspective: null,
    dataSelection: [],
  },
};

export default widgetIncidentPriority;
