import type { TemplateWidget } from '../template';

const widgetContainerReferences: TemplateWidget = {
  name: 'Container References',
  widget: {
    type: 'attribute',
    id: 'widgetContainerReferences',
    perspective: null,
    dataSelection: [],
  },
};

export default widgetContainerReferences;
