import type { TemplateWidget } from '../template';

const widgetContainerName: TemplateWidget = {
  name: 'Container Name',
  widget: {
    type: 'attribute',
    id: 'widgetContainerName',
    perspective: null,
    dataSelection: [],
  },
};

export default widgetContainerName;
