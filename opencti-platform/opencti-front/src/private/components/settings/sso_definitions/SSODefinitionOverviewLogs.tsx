import { Grid2 as Grid } from '@mui/material';
import React from 'react';
import Card from '@common/card/Card';

// interface SSODefinitionOverviewMappingProps {
//   logsSso: SSODefinition;
// }

const SSODefinitionOverviewLogs = (
  // { logsSso }: SSODefinitionOverviewMappingProps
) => {
  // const theme = useTheme<Theme>();
  // const { t_i18n, fldt, n } = useFormatter();
  // const logssso = useFragment(detailsFragment, logsSso);

  return (
    <Grid size={{ xs: 12 }}>
      <Card
        title="LOGS"
      >
        TEST
      </Card>
    </Grid>
  );
};

export default SSODefinitionOverviewLogs;
