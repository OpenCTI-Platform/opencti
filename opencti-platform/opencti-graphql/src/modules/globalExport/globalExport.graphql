input GlobalExportSelectionInput {
  entityType: String!
  ids: [ID!]
}

extend type Query {
  globalConfigurationExport(entityTypes: [String!]!, selections: [GlobalExportSelectionInput!]): String! @auth(for: [BYPASS])
}